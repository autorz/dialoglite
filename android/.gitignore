# Caches do build containerizado (docker/build.sh) — nunca versionar.
.build-cache/
.gradle/
build/
app/build/
local.properties
keystore.properties
*.keystore
*.jks
captures/
.externalNativeBuild/
.cxx/

# Nota historica: o .gitignore da raiz tinha `data/` (o diretorio SQLite do app
# Flask), e esse padrao casava com QUALQUER diretorio `data` — inclusive o
# pacote Kotlin `me.zippert.dialoglite.data`, cujos arquivos novos sumiam do
# commit em silencio. Corrigido na raiz, ancorando o padrao em `server/data/`,
# entao a negacao que vivia aqui saiu. Se o sintoma voltar, e la que se olha.
