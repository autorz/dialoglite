# Flask Configuration
# Generate a secure key using: python -c 'import os; print(os.urandom(24).hex())'
SECRET_KEY=

# Database Configuration
# Default is sqlite:///data/app.db (ensure the 'data' directory exists and is writable)
DATABASE_URI=sqlite:///data/app.db

# Flask Environment (development or production)
FLASK_ENV=production
